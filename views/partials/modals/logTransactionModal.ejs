<div id="logTransactionModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <span class="close">&times;</span>
            <h2>New Transaction for</h2>
        </div>
        
        <form id="logTransactionForm">
            <label for="transactionName">Name:</label>
            <input class="modal-input" type="text" id="transactionName" name="transactionName" required>
        
            <label for="transactionAmount">Amount (£):</label>
            <input class="modal-input" type="number" id="transactionAmount" name="transactionAmount" required>

            <label for="transactionDate">Date (optional):</label>
            <input class="modal-input" type="date" id="transactionDate" name="transactionDate">

            <div class="modal-row">
                <div class="modal-item">
                    <label for="transactionType">Type:</label>
                    <select class="modal-input" id="transactionType" name="transactionType" required>
                        <option value="" selected>-- Select Type --</option>
                        <option value="income">Income</option>
                        <option value="expense">Expense</option>
                    </select>
                </div>
                
                <div class="modal-item">
                    <label for="transactionCategory">Category:</label>
                    <select class="modal-input" id="transactionCategory" name="transactionCategory" required>
                        <option value="" selected>-- Select Category --</option>
                        <% Object.keys(transactionCategories).forEach(key => { %>
                            <option value="<%= key %>"><%= transactionCategories[key] %></option>
                        <% }) %>
                    </select>
                </div>
            </div>
        
            <label for="recurring">
                <input type="checkbox" id="recurring" name="recurring">
                This transaction is recurring (will use date of transaction as recurring date)
            </label>
        
            <button type="submit" class="btn">Log Transaction</button>
        </form>
    </div>
</div>

<script>

app.post('/log-transaction', (req, res) => {
    const { transactionName, transactionAmount, transactionType, transactionCategory, id, type } = req.body;

    // Determine the type of item based on which ID is present
    if (potId) {
        // Handle transaction for pot
    } else if (accountId) {
        // Handle transaction for account
    } else if (budgetId) {
        // Handle transaction for budget
    }

    // Save transaction and redirect or respond
    res.redirect('/somewhere');
});
</script>